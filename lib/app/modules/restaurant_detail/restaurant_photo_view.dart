import 'package:flutter/material.dart';

class RestaurantPhotoView extends StatelessWidget {

  const RestaurantPhotoView({ Key? key }) : super(key: key);

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text(''),),
           body: Container(),
       );
  }
}